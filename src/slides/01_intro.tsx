import { type ReactElement } from 'react'

import { RevealSlides } from '../components/reveal'

export function IntroSlides (): ReactElement {
    return <RevealSlides>
        <section>{'adsfdsg111'}</section>
        <section>{'bdsfasd'}</section>
    </RevealSlides>
}
